import React from 'react';

const NewMuteButton = (props) => { 
		console.log("PR********************S", props);
        return (
         <div>
             
         </div>
)};

export default NewMuteButton;